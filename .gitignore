# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release
Project Documents
pubspec.lock
pubspec.lock
.env
pubspec.lock
documents
ios/Podfile.lock
MIGRATION_SUMMARY.md
ROOM_MANAGEMENT_FEATURE.md
devtools_options.yaml

# ข้อมูลผู้เช่าจริงที่ export มาจากฐานข้อมูล — ชื่อ เบอร์โทร เลขห้อง
# ห้ามเข้า git เด็ดขาด ไฟล์ที่เคยขึ้นไปแล้วลบทีหลังยังอยู่ในประวัติตลอดไป
database/csv/
sample_documents/

# ไฟล์ที่ส่วนขยายของ editor สร้างเอง ไม่ใช่ของโปรเจกต์นี้ (java-upgrade ในโปรเจกต์
# Flutter) · ถ้าวันไหนเพิ่ม GitHub Actions จริง ให้ยกเว้น .github/workflows/ ออกมา
.github/
